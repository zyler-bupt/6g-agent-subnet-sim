numpy>=1.23

# Optional plotting for experiments.run. The experiment still writes CSV if
# matplotlib is unavailable.
matplotlib>=3.8

# Semantic controller API and schemas.
pydantic>=2.0
aiohttp>=3.9

# Optional SANet baselines.
torch>=2.0
